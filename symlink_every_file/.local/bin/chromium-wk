#!/bin/bash

# Shim, rofi does not read this dir (ubuntu 16.04)
cd ~ && exo-open ~/.local/share/applications/chromium-work.desktop
#!/bin/bash

#/usr/bin/chromium-browser --password-store=basic --user-data-dir=.config/google-chrome-work/ "$@"
